<!--index.wxml-->
<view class="container">
  <view wx:key="value" wx:for="{{ data }}">
    {{ item.title }}
  </view>
  <c-like 
    count="{{ data.length }}" 
    bind:onBtnClick="onBtnClick"
  />  
</view>
